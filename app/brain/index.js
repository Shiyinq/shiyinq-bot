const mainMenu = require('./mainMenu')
const smalltalk = require('./smalltalk')
const fallback = require('./fallback')

module.exports = {
  smalltalk: smalltalk,
  mainMenu: mainMenu,
  fallback: fallback
}
